
## 修改意见 1.0

### 1. 菜单重组

- 侧边栏分组`系统`改为`规则配置`（仅作分组标题，不可点击）
- 原`规则配置`菜单项改名为`门禁配置`，归属`规则配置`分组下（如`任务看板`于`研发`下）
- `技能市场`从原`系统`分组下独立，成为与`规则配置`同级的单独菜单
- 新增`智能体`菜单，与`技能市场`同级，展示平台内置 8 个 Agent（需求分析 Agent、代码生成 Agent 等）的管理页面

目标菜单结构：
```
主菜单
  ├─ 工作台
  └─ 项目空间
研发
  ├─ 任务看板
  └─ 阶段流水线
质量
  ├─ 研发追溯（原"门禁管理"，见第 7 条）
  └─ 评审中心
规则配置（分组标题，不可点击）
  └─ 门禁配置（原"规则配置"）
智能体（独立菜单）
技能市场（独立菜单）
```

### 2. 门禁配置可添加

用户可在任一已有规则分类下添加新规则。规则字段：

| 字段 | 说明 | 必填 |
|------|------|------|
| 规则名称 | 如"代码复杂度检查" | 是 |
| 所属阶段 | 下拉选择（需求分析/需求拆解/方案设计/代码生成/代码审查/单元测试/质量检查/验收确认） | 是 |
| 规则描述 | 说明该规则检查什么 | 是 |
| 门禁类型 | pass（阻断）/ warn（警告） | 是 |
| 检查方式 | 自动/手动 | 是 |
| 是否启用 | 默认开启 | 是 |

### 3. 评审中心增强

- 每条评审项增加所属项目信息
- 修复`查看详情`按钮 bug（引用不存在的任务 t9 导致跳转失败）

### 4. 新增"研制总结"页面

- 新增页面菜单`研制总结`
- 包含 3 个定时任务，用户可分别为 3 个任务设置定时周期：
  1. 统计平台用量
  2. 代码提交量
  3. 需求数量
- 当前阶段做静态模拟（展示模拟数据）

### 5. 新建项目空间删除 Harness 规则集配置

- 新建项目时去掉"Harness 规则配置集"下拉框
- 项目卡片上也去掉规则集标签
- `待定`：是否有其他配置项需要补充

### 6. 工作台增加更多统计内容

在现有 4 个统计卡基础上增加以下维度：

1. **门禁通过率** — 质量门禁的整体通过比例，直接反映代码质量水位
2. **需求交付周期**（Lead Time）— 从需求提出到交付的平均天数，反映研发效率
3. **缺陷逃逸率** — 逃过门禁进入生产环境的缺陷比例，反映门禁有效性
4. **AI 编写代码的提交数** — AI 生成代码的提交占比，反映 AI 辅助研发效能

### 7. 门禁管理改为"研发追溯"

- 菜单名从`门禁管理`改为`研发追溯`
- 用途：给人类审计使用，展示任务从开始到结束的完整证据链
- 表现形式：**时间线证据链**（方案 A）

示例结构：
```
需求分析 ✓ ──── 方案设计 ✓ ──── 代码生成 ✓ ──── 单元测试 ✗
  │                │                │                │
  ├ 需求完整性 ✓   ├ 架构合规 ✓    ├ 编码规范 ✓    ├ 覆盖率 62% < 80%
  ├ 语义冲突 ✓     ├ 技术选型 ✓    ├ 安全扫描 ✓    ├ [截图:覆盖率报告]
  └ 追溯性 ✓       └ [截图:架构图] └ [截图:代码]   └ 复杂度超标
```

- 每个阶段是一个节点，展开后显示门禁项 + 证据（文字/截图）
- 截图作为证据链的一部分（如测试覆盖率报告截图、代码审查截图、架构图截图等）
