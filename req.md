
## 修改意见 1.0

> **状态：全部完成** (2026-05-15)

### 1. 菜单重组 ✅

- 侧边栏分组`系统`改为`规则配置`（仅作分组标题，不可点击）
- 原`规则配置`菜单项改名为`门禁配置`，归属`规则配置`分组下
- `技能市场`独立为单独菜单
- 新增`智能体`菜单，展示平台内置 8 个 Agent 管理页面
- 新增`研制总结`菜单（见第 4 条）

实际菜单结构：
```
主菜单
  ├─ 工作台
  └─ 项目空间
研发
  ├─ 任务看板
  └─ 阶段流水线
质量
  ├─ 研发追溯（原"门禁管理"）
  └─ 评审中心
规则配置（分组标题，不可点击）
  └─ 门禁配置
智能体（独立菜单）
研制总结（独立菜单）
技能市场（独立菜单）
```

**修改文件**：`router.js`（renderPage switch）、`pages.js`（renderAgents）、`index.html`（侧边栏）

### 2. 门禁配置可添加 ✅

用户可在任一已有规则分类下添加新规则。规则字段：

| 字段 | 说明 | 必填 |
|------|------|------|
| 规则名称 | 如"代码复杂度检查" | 是 |
| 所属阶段 | 下拉选择（需求分析/需求拆解/方案设计/代码生成/代码审查/单元测试/质量检查/验收确认） | 是 |
| 规则描述 | 说明该规则检查什么 | 是 |
| 门禁类型 | pass（阻断）/ warn（警告） | 是 |
| 检查方式 | 自动/手动 | 是 |
| 是否启用 | 默认开启 | 是 |

**修改文件**：`rules-config.js`（showAddRuleModal、addNewRule、动态 categories）

### 3. 评审中心增强 ✅

- 每条评审项增加所属项目名称
- 修复`查看详情`按钮 bug（引用不存在的任务 t9 导致跳转失败，增加 null 安全检查）

**修改文件**：`reviews.js`（renderReviews 重写）

### 4. 新增"研制总结"页面 ✅

- 新增页面菜单`研制总结`
- 展示 8 个统计卡（总任务/已完成/代码提交/需求/测试覆盖率/门禁通过率/AI代码比例/交付周期）
- 研发效能指标表 + AI 赋能统计表
- 任务阶段分布进度条 + 本周趋势表
- 当前为静态模拟数据

**修改文件**：`pages.js`（renderSummary、summaryStats）

### 5. 新建项目空间删除 Harness 规则集配置 ✅

- 新建项目时去掉"Harness 规则配置集"下拉框
- 项目卡片上也去掉规则集标签
- 初始数据中的 `rulesSet` 字段已移除

**修改文件**：`projects.js`（showNewProjectModal、renderProjectCards）

### 6. 工作台增加更多统计内容 ✅

在原有 4 个统计卡基础上新增第二行 4 个统计卡：
1. **门禁通过率** 91.7%
2. **需求交付周期** 8.5 天
3. **AI 代码提交数** 596
4. **缺陷逃逸率** 3.2%

新增两个内容区：
- **门禁健康度**：6 个阶段的通过率进度条
- **本周趋势**：新增任务/完成任务/门禁阻断/代码提交/AI代码提交的周环比

**修改文件**：`dashboard.js`（renderDashboard）

### 7. 门禁管理改为"研发追溯" ✅

- 菜单名从`门禁管理`改为`研发追溯`
- 用途：给人类审计使用，展示任务从开始到结束的完整证据链
- 最终实现：**方案 D — 审计报告卡片**

每张审计报告包含：
- 报告头部（任务信息、项目、需求来源、审计员、检查时间）
- 阶段审计区块（门禁项 + 检查结果描述 + 证据文件占位）
- 审计结论（阶段完成数、阻断数、通过率）
- 追溯链路（箭头串联阶段，阻断标红）
- 卡片折叠效果（点击头部展开/收起）

**修改文件**：`gates.js`（renderGates、renderAuditCard、toggleAuditCard、refreshAllGates）

---

## 附加优化

### 研发追溯表现形式方案选型 ✅

对比了两个方案后选择方案 D：
- **方案 B**：需求驱动追溯树（树形结构，需求→阶段→门禁项）
- **方案 D**：审计报告卡片（正式报告格式，适合审计场景）← 选定

通过 git worktree 并行实现两个方案，合并方案 D 回主分支。

### 研发追溯卡片折叠效果 ✅

- 默认收起，只显示报告头部摘要（任务信息 + 状态标签）
- 点击头部展开显示阶段审计详情 + 审计结论 + 追溯链路
- 箭头指示展开状态，带旋转动画
- 展开区域使用 max-height 过渡动画

**修改文件**：`gates.js`（toggleAuditCard）

### 单文件拆分为多文件 ✅

将原 2929 行 `index.html` 拆分为 13 个文件，减少 coding agent 读写大文件的 token 消耗：

| 文件 | 行数 | 职责 |
|------|------|------|
| index.html | 86 | HTML 骨架 + script/link 引用 |
| styles.css | 363 | 全部 CSS 样式 |
| data.js | 187 | state 数据 + 工具函数 |
| router.js | 171 | 导航 + 路由 + init |
| dashboard.js | 104 | 工作台页面 |
| projects.js | 65 | 项目空间页面 |
| kanban.js | 223 | 任务看板 + 拖拽 |
| pipeline-render.js | 684 | 流水线渲染 |
| pipeline-actions.js | 471 | 流水线操作 |
| gates.js | 249 | 研发追溯（审计报告卡片） |
| reviews.js | 40 | 评审中心 |
| rules-config.js | 122 | 门禁配置 |
| pages.js | 148 | 智能体 + 研制总结 + 技能市场 |

加载顺序：`data.js` → 各页面 JS → `router.js`
