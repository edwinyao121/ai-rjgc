## 规范界面修改
1. 全局层面的规范改名为`规范约束`
2. 项目层面的规范，把`通用规范`和`专用规范`，分为tab页
3. 全局层面的`通用规范`也应该展示默认适用哪些智能体
4. 去掉专用规范中的“测试与质量要求”
5. ”项目规范配置与校验中心“，改名去掉校验中心
6. 规范应该配置开关按钮来,项目层面的`通用规范`应该展示全局的`通用规范`的所有
7. `架构设计` 询问用户，项目改的话落成实际。
8. 删除`智能体 System Prompt 注入可观测面板`



## 其他
1. `项目空间界面` 中将公文管理系统放到第一个
2. 修改`任务监控`定时，时间增加。
3. 任务流程修改阶段,"质量检查"改为"UI测试",”验收确认“改为”生成制品“
4. 取消点击事件：右侧抽屉不再弹出。
5. 把`技能市场`的`演示调用`按钮取消
6. 项目层面的`项目智能体`和`项目技能库`需要参照全局层面


