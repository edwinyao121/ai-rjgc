# 项目协作规则

## 设备范围

- 本项目是桌面端优先的原型。默认不处理移动端适配、移动端断点、移动端导航或移动端截图。
- 只有用户明确提出移动端需求时，才扩大范围评估或修改移动端体验。
- 默认验证视口使用桌面端尺寸，优先检查首屏信息层级、主流程和桌面端交互。

## 软件工程文档场景

- `云端治理与本地智能研发协同平台_UseCase与UML.md` 主要体现“生成与智能编码一致的软件工程文档”场景。
- 文档聚焦“需求—代码—研发事实—文档”闭环，弱化项目初始化、组织级知识管理、复杂多智能体调度和自动审批等无关用例。
- 参与者只保留：项目负责人、开发人员、云端总控智能体、本地智能体、质量人员、外部系统（Git、制品仓库、流水线等）。

## 云端与本地职责边界

- 本地客户端负责采集研发过程记录、研发产物并上报云端。
- 云端总控智能体只辅助项目负责人和质量人员进行云端决策，不直接调用、指挥或与本地智能体交互。

## 用例文档规范

- 核心用例使用 `## UC-xx` 标题，并采用固定表格格式。
- 每个用例表格必须包含：用例目标、主要参与者、前置条件、基本流程、强制升级、输出。
- `Harness Run` 优先表述为“执行任务”；描述对应实例时使用“任务执行实例”。

## UML 文档范围

- 文档只保留 UML 端到端活动图、UML 核心时序图和 UML 组件图。
- 不保留 UML 用例图、领域模型图、任务状态图和任务执行实例状态图。

## 术语与变更要求

- 中文优先，减少中英混杂；仅保留 UML、Git、MCP、AI、GJB、PlantUML 等必要缩写或专有名词。
- 文档修改完成后，执行结构和格式校验，并提交 Git 记录。
