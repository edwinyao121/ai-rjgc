# 快速生成与云原生交付需求文档

> 文档版本：2.0  
> 编制日期：2026-07-10  
> 文档状态：MVP 需求基线建议稿  
> 适用范围：AISF / Harness 智能研发平台  
> 副标题：基于工程等级、风险等级与 Harness 控制的智能研发闭环

---

## 1. 文档说明

### 1.1 文档目的

本文定义 AISF / Harness 智能研发平台中的“快速生成与云原生交付”场景需求，明确场景理解、总体业务流程、快速生成流程、云原生交付流程、分级控制机制、功能边界、核心对象、状态流转、MVP 范围及验收标准。

本文重点回答以下问题：

1. 快速生成解决什么业务问题，生成结果应达到什么程度。
2. 云原生交付解决什么业务问题，交付物应包含哪些内容。
3. 快速生成与云原生交付如何形成从需求到运行环境的完整闭环。
4. 不同工程等级为什么需要不同的生成深度。
5. 不同风险等级为什么需要不同的门禁、审批、验证和证据要求。
6. Harness 在生成、验证、修复、交付和追溯过程中承担什么职责。
7. 第一阶段应实现什么，明确不实现什么。
8. 如何通过可观测、可量化、可追溯的标准判断建设是否成功。

本文不展开数据库表结构、接口定义、页面原型、智能体 Prompt、流水线脚本和具体研发任务拆分。进入研发前，应基于本文形成总体设计、数据模型、接口方案、Agent Runtime 方案、流水线方案和迭代任务清单。

### 1.2 场景定位

“快速生成与云原生交付”不是单纯的代码生成器、脚手架生成器、低代码平台，也不是将应用打成 Docker 镜像的单点工具。

它面向的是 AI 辅助研发下的一条完整业务主线：

```text
业务需求输入
  -> 系统理解目标和约束
  -> 智能体快速生成应用成果
  -> 自动构建、测试与反馈修复
  -> 按风险执行分级门禁和人工确认
  -> 形成镜像、部署资源、配置和验证结果
  -> 部署到目标环境或形成离线交付包
  -> 归档需求到交付物的证据链
```

其中：

- **快速生成**解决“应用如何更快形成”。
- **云原生交付**解决“应用如何稳定、标准、可验证地交出去”。
- **Harness 控制**解决“生成和交付过程如何可信、可控、可追溯”。
- **工程等级**决定生成深度和成果完整度。
- **风险等级**决定门禁、审批、验证和证据强度。

### 1.3 核心价值

本场景需要解决的核心问题不是“AI 能不能写代码”，而是：

- 用户提出自然语言需求后，能否快速形成可运行、可验证的应用成果。
- 需求不明确时，能否通过验证型原型降低需求走偏风险。
- 简单低风险场景能否快速得到真实可用的小型应用，而不被重型流程拖慢。
- 正式生产需求能否在项目规范、验收标准和质量门禁约束下完成实现。
- 存量项目变更和缺陷修复能否基于真实代码上下文完成影响分析、修改和回归验证。
- 生成结果能否同步形成测试、配置、镜像构建和部署资源，而不是交由 DevOps 和运维事后补齐。
- 测试、构建或部署失败后，是否能够诊断原因并驱动智能体受控修复。
- 代码、镜像、部署资源和交付包是否保持版本一致。
- 对外交付或完全离线环境中，是否能够形成完整依赖闭包并提前验证。
- 需求、计划、代码、测试、镜像、部署和人工决策是否能够形成可信证据链。

### 1.4 建设原则

1. **以业务主线为中心**：快速生成和云原生交付是业务主线，分类、门禁和 Harness 是支撑机制。
2. **生成必须形成可运行成果**：不能以文件生成完成作为成功，必须执行与工程等级匹配的验证。
3. **交付必须面向目标环境**：不能只交付程序包，应形成可构建、可部署、可验证、可回滚的交付单元。
4. **先计划后执行**：正式生成前形成执行计划；生产级、高风险和存量变更场景必须人工确认。
5. **约束强度与风险匹配**：低风险使用轻量约束，高风险、对外交付和强审计场景使用严格约束。
6. **分类维度保持正交**：工作类型、需求成熟度、工程等级、风险等级和交付方式分别判断。
7. **失败必须可处理**：支持诊断、受限自动修复、人工接管、回退、取消和重新规划。
8. **版本必须一致**：代码、配置、镜像、部署资源、交付包和证据绑定明确版本或摘要。
9. **人工决策必须留痕**：确认、驳回、风险接受、批准和回退均绑定对象版本和责任人。
10. **不得通过降级绕过门禁**：约束降级只能基于业务目标或风险条件变化，并经过授权审批。
11. **MVP 优先验证闭环**：第一阶段优先验证“快速生成—自动验证—反馈修复—容器交付”的稳定闭环。

### 1.5 术语说明

| 术语 | 定义 |
| --- | --- |
| 快速生成 | 根据需求、工程等级和项目规范，快速形成代码、测试、配置、运行说明及必要交付资源的过程。 |
| 云原生交付 | 将应用形成可构建、可部署、可验证、可回滚、可追溯交付单元的过程。 |
| Harness | 对智能体研发过程实施计划、约束、调度、监控、门禁、回退和证据归档的控制层。 |
| 项目空间 | 承载需求、项目规范、代码上下文、运行记录、产物和证据的逻辑边界。 |
| 项目规范 | 对技术栈、工程结构、编码、测试、安全、构建和交付方式的版本化约束。 |
| 执行策略 | 系统基于工作类型、需求成熟度、工程等级、风险等级和交付方式形成的结构化执行配置。 |
| Harness Run | 一次从计划确认到生成、验证、评审、交付和归档的受控执行实例。 |
| 工程等级 | 表示生成成果需要达到的完整度，包括验证原型、轻量可用和生产级。 |
| 风险等级 | 表示业务、数据、运行、合规和交付风险，包括低风险、标准风险和高风险。 |
| 交付基线 | 已锁定需求范围、代码版本、镜像摘要、部署资源和验证结果的可交付版本集合。 |
| 门禁 | 对产物或执行结果进行自动或人工判定的结构化规则。 |
| 证据 | 由系统采集或人员确认、用于证明执行事实和结果的结构化记录。 |

---

## 2. 场景理解

### 2.1 快速生成是什么

快速生成是从业务需求到可运行应用成果的智能化生产过程。它不仅生成代码，还应根据工程等级按需形成测试、配置、运行说明、构建配置和部署资源。

快速生成需要解决以下问题：

- 将自然语言需求转为结构化需求项、验收标准和执行计划。
- 识别新建应用、存量变更和缺陷修复等不同工作类型。
- 根据需求成熟度判断是否需要先形成验证型原型。
- 根据工程等级决定生成成果的完整度。
- 绑定项目规范和真实代码上下文，避免仅依赖 Prompt 自由生成。
- 对生成结果执行启动、测试或回归验证。
- 将失败结果反馈给智能体，形成受控修复循环。
- 将低置信推断、未验证假设和非生产内容显式标注。

快速生成不等于“一键生成完整生产系统”，也不意味着所有需求都进入相同流程。

### 2.2 快速生成的三种工程等级

#### 2.2.1 验证原型

适用于需求探索和业务方向验证。

重点目标：

- 验证业务流程是否成立。
- 验证页面和交互是否符合预期。
- 验证数据对象、字段和接口边界是否合理。
- 发现待澄清规则和潜在冲突。
- 形成可迁移到生产阶段的需求资产。

允许使用 Mock 数据、临时接口、简化权限和简化异常处理，但必须形成非生产项清单。

#### 2.2.2 轻量可用

适用于个人、小团队、低并发、低敏感度和低审计要求的真实应用。

重点目标：

- 快速形成可启动、可使用的小型应用。
- 核心业务流程可完成。
- 有基础错误处理和配置说明。
- 关键数据可导出、备份或恢复。
- 提供本地、单机或简单容器运行方式。

轻量可用应用不是原型，但不默认要求完整 Kubernetes 资源、镜像扫描、离线包和完整证据链。

#### 2.2.3 生产级

适用于正式团队使用、对外服务、长期运行或需要持续维护的应用。

重点目标：

- 需求项与验收标准可追溯。
- 代码符合项目和架构规范。
- 关键流程具备测试覆盖。
- 配置、安全、构建和部署资源完整。
- 通过标准或严格门禁。
- 可进入测试、预生产或生产环境。
- 具备版本记录、回滚方式和交付证据。

### 2.3 云原生交付是什么

云原生交付不是简单生成 Dockerfile，也不是要求所有应用必须使用 Kubernetes。

云原生交付是指：系统根据应用工程等级、风险等级、运行环境和交付目标，将生成结果转化为可构建、可部署、可验证、可回滚、可追溯的应用交付单元。

交付物可按场景包含：

- 应用代码和构建配置。
- 自动化测试和验证脚本。
- Dockerfile 或镜像构建配置。
- 容器镜像、标签和摘要。
- Kubernetes YAML、Helm Chart 或其他部署资源。
- 配置模板和 Secret 占位说明。
- 环境变量、端口、存储和外部依赖说明。
- 健康检查和资源限制。
- 部署、验证和回滚脚本。
- 测试、扫描和部署验证结果。
- 版本说明、校验和、SBOM 和交付证据索引。

### 2.4 云原生交付与传统交付的差异

传统交付通常是：

```text
开发完成代码
  -> DevOps 补构建配置
  -> 运维补部署资源
  -> 人工修改环境配置
  -> 部署后人工排查
  -> 人工整理交付记录
```

智能云原生交付应形成：

```text
需求项和验收标准
  -> 生成代码、测试、配置和构建资源
  -> 自动构建与质量门禁
  -> 生成镜像和部署资源
  -> 自动部署与验证
  -> 失败解释和反馈修复
  -> 锁定交付基线
  -> 归档需求到交付物的证据链
```

核心差异是：

- 从代码驱动转为需求驱动。
- 从只交付程序转为交付可运行应用单元。
- 从人工补齐部署资源转为生成阶段同步形成。
- 从只判断部署成功转为判断需求、代码、镜像、部署和验证是否一致。
- 从失败依赖专家排查转为失败可解释、可定位、可反馈修复。

### 2.5 快速生成与云原生交付的关系

快速生成和云原生交付不是两套独立能力，而是一条连续业务链。

```text
快速生成
解决“应用如何更快形成”
        ↓
自动验证与反馈修复
解决“生成结果是否真实可用”
        ↓
分级门禁与人工控制
解决“结果是否可信、能否继续推进”
        ↓
云原生交付
解决“应用如何稳定交出去并进入运行环境”
```

快速生成阶段的主要差异来自工程等级；云原生交付阶段的主要差异来自风险等级和交付方式。

### 2.6 Harness 的控制作用

Harness 不替代智能体执行具体编码，也不替代人员做业务决策。其职责是：

- 接收需求、项目规范和交付约束。
- 形成执行策略和阶段计划。
- 为智能体准备真实代码、知识和环境上下文。
- 限定智能体可使用的工具、目录、命令和资源。
- 记录生成、工具调用、文件变更和验证过程。
- 执行门禁、人工确认和阶段推进。
- 在失败时组织诊断、修复、重试、回退或人工接管。
- 将需求、产物、验证结果和人工决策关联为证据链。

责任边界如下：

```text
用户和负责人：确定业务目标、关键规则、验收标准和最终决策。
智能体：完成分析、生成、修改、测试生成、失败解释和修复建议。
Harness：完成计划、约束、调度、状态、门禁、回退、证据和责任边界控制。
工具链：完成代码存储、依赖获取、构建、测试、扫描、镜像、部署和制品归档。
```

### 2.7 分级策略模型

系统通过以下五个维度形成执行策略，而不是让用户直接选择一个含义混合的“模式”。

| 维度 | 作用 | 典型取值 |
| --- | --- | --- |
| 工作类型 | 决定代码上下文和执行路径 | 新建应用、存量变更、缺陷修复 |
| 需求成熟度 | 决定是否先进行需求验证 | 探索中、基本明确、已确认 |
| 工程等级 | 决定生成成果完整度 | 验证原型、轻量可用、生产级 |
| 风险等级 | 决定门禁、审批和证据强度 | 低风险、标准风险、高风险 |
| 交付方式 | 决定最终交付产物 | 本地、单机、容器、Kubernetes、离线包 |

执行策略示例：

```text
工作类型：存量功能变更
需求成熟度：已确认
工程等级：生产级
风险等级：高风险
交付方式：离线 Kubernetes
```

### 2.8 风险强制升级规则

以下条件具有强制升级权，不得仅依据使用人数少或用户希望快速交付而降低约束：

- 涉及敏感、涉密、个人隐私或重要业务数据。
- 涉及身份认证、授权、审批、支付或审计。
- 面向外部用户或跨组织交付。
- 涉及生产环境和长期运行服务。
- 涉及数据库结构迁移或不可逆数据变更。
- 涉及公共接口兼容性或多个系统联动。
- 涉及高可用、灾备或强可靠性要求。
- 需要完全离线交付或客户现场部署。
- 涉及强监管、强审计或责任追究要求。

---

## 3. 总体业务闭环

### 3.1 总体流程

```text
业务需求输入
  ↓
需求理解与约束识别
  ↓
确定工作类型、需求成熟度、工程等级、风险等级和交付方式
  ↓
生成执行策略与实现计划
  ↓
必要的人工确认
  ↓
智能体快速生成代码、测试、配置和交付资源
  ↓
自动构建、启动、测试或回归验证
  ↓
失败诊断与受控反馈修复
  ↓
按风险等级执行轻量、标准或严格门禁
  ↓
形成应用镜像、部署资源、配置模板和验证结果
  ↓
部署到目标环境并执行冒烟、健康或回滚验证
  ↓
在线发布或形成离线交付包
  ↓
锁定交付基线并归档证据
```

### 3.2 工程等级对流程的影响

#### 验证原型流程

```text
模糊需求
  -> 生成验证目标
  -> 快速生成页面、流程、Mock 接口和样例数据
  -> 轻量运行验证
  -> 用户反馈与待澄清项记录
  -> 形成非生产项清单
  -> 原型确认或继续修正
  -> 转生产级输入或终止
```

#### 轻量可用流程

```text
明确的低风险需求
  -> 生成简化实现计划
  -> 快速生成应用
  -> 启动校核和核心流程冒烟
  -> 基础配置与敏感信息检查
  -> 必要的自动修复
  -> 数据导出、备份或恢复说明
  -> 本地、单机或容器交付
```

#### 生产级流程

```text
已确认需求和验收标准
  -> 影响分析与完整实现计划
  -> 技术负责人确认
  -> 代码、测试、配置和部署资源生成
  -> 单元、集成或回归测试
  -> 代码、安全和构建检查
  -> 镜像构建与部署资源校核
  -> 环境部署验证
  -> 人工评审与发布批准
  -> 形成可追溯交付基线
```

### 3.3 风险等级对流程的影响

| 控制维度 | 低风险 | 标准风险 | 高风险 |
| --- | --- | --- | --- |
| 计划确认 | 可简化或抽样 | 正式执行计划确认 | 多角色确认或审批 |
| 测试验证 | 启动与核心冒烟 | 单元、集成、关键流程测试 | 完整回归、环境预检和必要专项测试 |
| 安全检查 | 敏感配置检查 | 基础代码和依赖安全检查 | 代码、依赖、镜像、配置和权限专项检查 |
| 部署验证 | 本地或容器启动 | 测试环境部署与冒烟 | 模拟目标环境、回滚和故障恢复验证 |
| 失败处理 | 自动修复后人工确认 | 修复、重试和人工评审 | 禁止绕过，需重新评估或风险审批 |
| 人工角色 | 需求发起人或负责人 | 技术负责人和质量人员 | 需求、技术、质量、交付多角色 |
| 证据要求 | 简化运行记录 | 完整生成、测试、构建和部署记录 | 完整证据链、版本摘要和批准记录 |

### 3.4 典型组合流程

#### 轻量可用 × 低风险

```text
需求输入
  -> 简化计划
  -> 应用生成
  -> 启动与冒烟
  -> 基础修复
  -> 本地或容器运行
  -> 简化生成说明和验证记录
```

#### 轻量可用 × 高风险

工程等级可以保持“轻量可用”，但风险等级会强制增加控制：

```text
需求输入
  -> 风险识别
  -> 正式计划确认
  -> 应用生成
  -> 权限、数据和安全校核
  -> 标准或严格门禁
  -> 受控环境部署
  -> 人工批准
  -> 完整证据归档
```

此类场景不能因为用户少或功能简单而自动采用轻量门禁。

#### 生产级 × 高风险 × 离线交付

```text
已确认需求
  -> 完整影响分析与计划审批
  -> 生产级代码、测试、配置和部署资源生成
  -> 严格测试、安全扫描和镜像扫描
  -> 目标环境预检
  -> 云环境或模拟环境部署
  -> 冒烟和回滚验证
  -> 锁定 commit、镜像 digest 和部署资源版本
  -> 生成离线交付包
  -> 完整性和校验和检查
  -> 交付批准与证据归档
```

### 3.5 异常和回退闭环

任何阶段失败后，不得直接标记为成功或可交付。

系统应根据失败类型提供：

- 失败原因分类。
- 关键日志和证据定位。
- 智能体修复建议。
- 受限次数的自动修复。
- 重新执行指定阶段。
- 人工修改后继续验证。
- 回退到上一有效产物或计划版本。
- 取消当前 Run。
- 重新规划执行策略。

自动修复达到上限后，必须进入人工接管或终止状态，不得无限循环消耗资源。

---

## 4. 快速生成业务流程

### 4.1 需求进入与目标理解

#### 输入

- 自然语言需求、需求文档或变更说明。
- 页面草图、交互说明或数据样例。
- 用户角色、使用范围和业务流程。
- 验收标准或验收线索。
- 已有项目代码和项目规范。
- 部署环境和交付约束。
- 数据敏感度、审计和长期运行要求。
- 明确的不做范围。

#### 处理

系统应：

- 识别业务目标、核心用户和主流程。
- 识别工作类型和需求成熟度。
- 提取功能需求、约束和待澄清问题。
- 识别必须人工确认的关键规则。
- 形成建议的工程等级、风险等级和交付方式。
- 说明推荐依据和进入更高等级所需条件。

#### 输出

- 结构化需求摘要。
- 初步验收标准。
- 待澄清问题。
- 执行策略建议。
- 风险与边界说明。

### 4.2 项目规范和上下文匹配

系统应在生成前准备：

- 技术栈及版本。
- 工程结构和模块边界。
- 编码、命名和 API 风格。
- 日志、异常和配置管理规范。
- 数据库、接口和兼容性约束。
- 测试和质量门禁要求。
- 构建、镜像和部署模板。
- 可用组件、SDK 和基础镜像。
- 允许访问的代码目录和工具权限。

生成任务必须绑定明确的项目规范版本。规范变化后，应能够识别受影响的执行计划、运行实例和交付基线。

### 4.3 执行计划生成与确认

执行计划至少应包含：

- 需求范围和不做范围。
- 影响模块和文件范围。
- 拟生成或修改的内容。
- 数据库、接口和配置影响。
- 测试和验证策略。
- 镜像与部署资源影响。
- 风险点和低置信推断。
- 阶段划分和任务依赖。
- 智能体、工具和权限需求。
- 预计交付物。

以下场景必须人工确认计划：

- 生产级工程。
- 标准风险或高风险场景。
- 存量项目变更。
- 数据库结构、公共接口和权限变更。
- 对外交付或离线交付。

### 4.4 验证原型生成

系统应支持：

- 生成可运行页面和交互流程。
- 生成 Mock 数据和临时接口。
- 形成原型验证目标。
- 记录用户反馈、确认结论和待澄清问题。
- 显式标注 Mock、临时接口、简化权限、未安全加固和未执行正式门禁等非生产项。
- 将已确认流程、页面、接口和数据转为生产级输入。

未确认原型不得直接作为生产级代码发布。

### 4.5 轻量应用快速生成

系统应支持：

- 生成可运行的小型应用。
- 提供本地、单机或简单容器运行方式。
- 生成基础配置样例和使用说明。
- 检查敏感信息硬编码。
- 执行启动和核心流程冒烟验证。
- 提供关键数据导出、备份或恢复说明。
- 标注不适合高并发、强审计、对外交付或完全离线部署等使用边界。

### 4.6 生产级快速生成

系统应支持围绕已确认计划生成或修改：

- 业务代码。
- 单元、集成或回归测试。
- API 定义和接口契约。
- 数据库迁移脚本。
- 配置文件和配置模板。
- Dockerfile 或构建配置。
- Kubernetes 或其他部署资源。
- 健康检查和资源限制。
- 生成说明和风险标注。

生产级生成结果必须关联需求项、验收标准、项目规范版本和代码版本。

### 4.7 存量项目变更

存量项目变更流程应包括：

```text
获取代码和分支上下文
  -> 分析影响范围
  -> 识别接口、数据、配置和模块变化
  -> 形成修改计划
  -> 人工确认
  -> 在隔离工作区生成变更
  -> 执行增量测试和回归测试
  -> 生成差异、风险和评审说明
  -> 门禁与人工评审
```

系统不得默认直接修改主分支，应支持工作区、临时分支或等效隔离机制。

### 4.8 缺陷修复

缺陷修复流程应包括：

- 接收错误现象、日志、复现步骤和预期结果。
- 定位可能影响模块和根因。
- 生成最小修复计划。
- 补充能够复现缺陷的测试。
- 生成修复代码。
- 执行缺陷复现、修复验证和相关回归。
- 输出根因、修改说明和剩余风险。

不得仅以错误消失作为成功，应确认关键回归范围未被破坏。

### 4.9 自动验证与反馈修复

系统应根据工程等级执行：

- 构建检查。
- 应用启动检查。
- 核心流程冒烟测试。
- 单元、集成或回归测试。
- 静态检查。
- 基础安全检查。
- 部署资源语法和引用检查。

失败后应：

1. 分类失败原因。
2. 提取关键日志和上下文。
3. 将失败证据反馈给对应智能体。
4. 生成修复计划或修复变更。
5. 重新执行受影响的最小验证集。
6. 达到重试上限后转人工接管。

---

## 5. 云原生交付业务流程

### 5.1 交付方式选择

系统应根据工程等级、风险等级、部署目标和运维要求选择交付方式：

| 交付方式 | 适用场景 | 主要产物 |
| --- | --- | --- |
| 本地运行 | 原型、个人工具 | 源码、启动命令、配置样例 |
| 单机部署 | 轻量内部应用 | 应用包、配置、启动和备份说明 |
| 简单容器 | 轻量或标准应用 | Dockerfile、镜像或 Compose 文件 |
| Kubernetes | 标准生产和高约束应用 | 镜像、Kubernetes 资源、配置和验证结果 |
| 离线交付 | 隔离环境或客户现场 | 镜像包、资源、脚本、文档、校验和和证据 |

云原生交付不等于所有场景都上 Kubernetes，但标准生产和高约束应用应具备容器化、版本化和自动验证能力。

### 5.2 构建配置生成

系统应支持生成或维护：

- Dockerfile。
- 构建上下文和忽略规则。
- 多阶段构建配置。
- 基础镜像和受控依赖源。
- 构建参数和环境变量占位。
- 镜像标签策略。
- 健康检查配置。

构建配置必须与应用变化同步更新，并遵循项目规范。

### 5.3 镜像构建与版本记录

镜像构建结果应记录：

- 镜像名称和标签。
- 镜像 digest。
- 构建时间和构建来源。
- 关联需求、Harness Run 和 commit。
- 基础镜像和依赖来源。
- 构建日志索引。
- 扫描结果或风险状态。
- 推送仓库和归档位置。

生产级和高风险场景不得仅使用可变 tag 作为唯一版本依据，应记录 digest。

### 5.4 部署资源生成

第一阶段可优先支持：

- Deployment。
- Service。
- ConfigMap。
- Secret 模板。
- Ingress 或访问配置。
- 健康检查。
- 环境变量。
- 资源请求和限制。
- 镜像版本引用。

后续可扩展：

- Helm Chart。
- Kustomize。
- HPA。
- NetworkPolicy。
- ServiceAccount 和 RBAC。
- 灰度发布和回滚策略。

### 5.5 配置与环境管理

系统应：

- 将环境相关配置与代码解耦。
- 生成配置模板而不是携带研发环境真实密钥。
- 标识必填项、默认值和敏感项。
- 检查端口、存储、证书、域名和外部服务依赖。
- 记录不同环境的配置版本。
- 支持部署前环境预检。

### 5.6 部署验证

部署完成后应记录：

- 部署环境和集群信息。
- 应用版本、commit 和镜像摘要。
- 部署资源版本。
- 部署开始和结束时间。
- 工作负载、Pod 和健康状态。
- 冒烟测试和访问验证结果。
- 配置和依赖检查结果。
- 失败原因和修复建议。
- 回滚目标和回滚结果。

冒烟测试或硬门禁失败时，不得标记为部署通过。

### 5.7 回滚与恢复

标准生产和高风险场景应支持：

- 确定上一可用交付基线。
- 记录可回滚镜像和部署资源版本。
- 生成或配置回滚方式。
- 对涉及数据库的变更提供回滚或前向恢复方案。
- 在要求严格的场景中执行回滚验证。
- 记录回滚原因、执行人和结果。

### 5.8 离线交付分析

第一阶段应优先支持离线交付分析，而非直接承诺所有环境真实打包和部署。

离线分析应识别：

- 所有应用和基础镜像。
- Kubernetes 资源引用的镜像。
- 配置、端口、存储、证书和外部服务依赖。
- Maven、npm、系统包或其他运行依赖。
- 目标环境版本、资源、网络和权限条件。
- 需要导出的脚本、文档、报告和制品。
- 当前版本基线是否已完成部署验证。

### 5.9 离线交付包生成

真实离线包应来自已验证的交付基线，建议目录结构：

```text
delivery-package/
  images/
  manifests/
  charts/
  configs/
  scripts/
  docs/
  checksums/
  sbom/
  reports/
  release-notes/
  deploy.sh
  verify.sh
  rollback.sh
```

离线包应至少包含：

- 镜像 tar 包或等效离线镜像制品。
- Kubernetes YAML 或 Helm Chart。
- 目标环境配置模板。
- 镜像导入、部署、验证和回滚脚本。
- 版本说明和依赖清单。
- 校验和文件。
- 部署手册和验收检查表。
- 交付证据索引。

### 5.10 离线包完整性校核

系统应检查：

- 所有部署资源引用的镜像均已包含。
- 镜像 tag、digest 和资源引用一致。
- 配置模板覆盖目标环境必填项。
- 脚本、文档和校验和完整。
- 离线包关联明确需求范围、commit 和交付基线。
- 软件包不包含研发环境密钥和敏感配置。
- 在条件允许时，可在隔离命名空间或模拟环境中完成部署验证。

---

## 6. 分级门禁与人工控制

### 6.1 门禁等级

#### 轻量门禁

适用于低风险验证原型和轻量可用应用：

- 构建或启动成功。
- 核心流程冒烟通过。
- 敏感配置未硬编码。
- 有基础错误处理。
- 有数据导出、备份或恢复说明。
- 有运行说明和适用边界。

#### 标准门禁

适用于正式团队使用的生产级应用：

- 需求和验收标准覆盖校核。
- 单元、集成或关键流程测试。
- 静态检查和基础安全检查。
- 镜像构建和版本记录。
- 部署资源检查和测试环境部署验证。
- 人工代码或技术评审。
- 部署结果和版本可追溯。

#### 严格门禁

适用于高风险、对外交付、离线部署、强审计和高可靠场景：

- 代码、依赖和镜像安全扫描。
- 镜像 digest 与部署资源一致性校验。
- 配置、权限和 Kubernetes 资源专项校核。
- 环境预检和容量条件检查。
- 完整回归和部署验证。
- 回滚验证。
- 离线包完整性校核。
- 多角色审批和完整证据链归档。

### 6.2 可执行门禁模型

每个门禁规则至少应包含：

```yaml
gate:
  id: smoke-test
  name: 核心流程冒烟测试
  appliesTo:
    engineeringLevels: [lightweight, production]
    riskLevels: [low, standard, high]
  executor: command
  command: npm run test:smoke
  timeoutSeconds: 300
  passCondition: exitCode == 0
  severity: hard
  evidence:
    - stdout
    - test-report
  onFailure:
    action: send_to_agent_fix
    maxRetry: 2
    requireHumanAfterRetry: true
```

### 6.3 硬门禁、软门禁和建议项

- **硬门禁**：失败后不得进入下一阶段或标记为可交付。
- **软门禁**：失败后可由授权人员进行风险接受，但必须说明原因、影响和有效范围。
- **建议项**：不阻断流程，但应记录优化建议。

涉及敏感数据泄露、关键测试失败、镜像缺失、部署失败和离线包完整性失败等情况原则上应作为硬门禁。

### 6.4 人工角色

| 角色 | 主要职责 |
| --- | --- |
| 需求确认人 | 确认业务目标、规则、范围和验收标准。 |
| 技术负责人 | 确认实现计划、架构影响、技术风险和工程等级。 |
| 质量评审人 | 确认测试范围、门禁结果和质量风险。 |
| 交付批准人 | 确认可发布版本、交付基线、回滚方式和对外交付条件。 |
| 平台管理员 | 维护项目规范、智能体、工具权限和门禁策略。 |

### 6.5 确认和批准规则

人工确认必须绑定：

- 被确认对象及版本。
- 内容摘要或哈希。
- 确认人和角色。
- 确认时间。
- 确认意见。
- 风险接受范围和有效期。

对象内容发生实质变化后，相关确认应自动失效或重新评估。

### 6.6 风险接受与约束调整

软门禁失败时，授权人员可以接受风险，但必须记录：

- 失败规则。
- 风险描述和影响范围。
- 补偿措施。
- 责任人和批准人。
- 有效版本和有效期。

不得为了绕过失败门禁而直接降低风险等级或工程等级。约束调整必须基于业务目标、交付范围或风险条件发生真实变化。

---

## 7. 功能需求

### 7.1 功能总览

功能按三组组织：

#### A. 快速生成能力

1. 需求理解与目标识别。
2. 执行策略和计划生成。
3. 项目规范与上下文准备。
4. 验证型原型生成。
5. 轻量应用生成。
6. 生产级代码、测试和配置生成。
7. 存量变更与缺陷修复。
8. 自动验证与反馈修复。

#### B. 云原生交付能力

1. 构建配置生成。
2. 镜像构建和版本记录。
3. 部署资源生成。
4. 配置与环境管理。
5. 部署验证。
6. 回滚与恢复。
7. 离线交付分析。
8. 离线包生成与完整性校核。

#### C. 可信控制能力

1. 项目规范管理。
2. Harness Run 编排与阶段推进。
3. 分级门禁。
4. 人工确认与审批。
5. 风险接受。
6. 过程可观测和人工接管。
7. 交付基线管理。
8. 证据归档和版本追溯。

### 7.2 需求理解与目标识别

作为快速生成入口，承接用户需求、项目上下文和交付约束，完成工作类型、需求成熟度、工程等级、风险等级和交付方式识别。

验收要求：

- 支持输入自然语言、文档、页面说明、接口草图和数据样例。
- 能够识别信息缺口和关键待确认规则。
- 能够推荐执行策略并说明推荐依据。
- 能够识别风险强制升级条件。
- 不得自动编造关键业务规则和验收结论。

### 7.3 执行策略与计划生成

作为 Harness 执行前置能力，承接结构化需求和项目规范，形成可确认、可调度和可追踪的执行计划。

验收要求：

- 计划包含需求范围、影响范围、任务、验证、风险和交付物。
- 支持人工确认、驳回和修订。
- 低置信推断和未验证假设必须标注。
- 确认后的计划应形成不可混淆的版本。

### 7.4 项目规范与上下文准备

作为智能体受控执行基础，提供技术栈、工程结构、编码规范、工具权限、知识上下文和运行环境。

验收要求：

- 项目规范可创建、审核、版本化和发布。
- Harness Run 必须绑定规范版本。
- 支持关联代码仓库、分支、组件仓库和环境信息。
- 智能体只能访问授权目录、命令和工具。

### 7.5 快速生成执行

作为应用成果生产能力，支持生成或修改代码、测试、配置和构建部署资源。

验收要求：

- 生成结果关联需求、计划、规范和 Run。
- 记录文件变更、工具调用和生成说明。
- 能够根据工程等级生成不同完整度的成果。
- 敏感信息不得写入代码、配置、镜像或交付包。
- 非生产项和低置信内容必须标注。

### 7.6 自动验证与反馈修复

作为生成结果可信性校核能力，执行构建、启动、测试、检查和部署验证，并将失败反馈给智能体修复。

验收要求：

- 支持配置验证命令、超时和通过条件。
- 失败结果包含分类、日志和证据。
- 支持自动修复次数上限。
- 支持仅重跑受影响验证项。
- 达到上限后转人工接管，不得无限循环。

### 7.7 Harness Run 编排与阶段推进

作为过程控制中心，承接执行计划，组织需求理解、生成、验证、门禁、交付和归档阶段推进。

验收要求：

- 支持阶段开始、暂停、恢复、重试、回退、取消和终止。
- 支持阶段依赖和进入条件。
- 支持人工确认节点和等待状态。
- 支持记录智能体、工具、文件和产物事件。
- 硬门禁未通过时不得推进。

### 7.8 构建、镜像和部署资源

作为云原生交付产物生成能力，形成镜像构建配置、镜像版本和部署资源。

验收要求：

- Dockerfile 与应用变更同步生成或更新。
- 镜像记录 tag、digest、commit 和构建日志。
- 部署资源引用明确镜像版本或摘要。
- 健康检查、配置模板和资源限制可按规范生成。
- 不得在 Secret 或配置中写入真实敏感信息。

### 7.9 部署验证与回滚

作为交付有效性验证能力，完成目标环境部署、健康检查、冒烟测试和回滚处理。

验收要求：

- 记录环境、版本、状态和验证结果。
- 冒烟或硬门禁失败不得标记部署通过。
- 标准生产和高风险场景支持上一可用基线回滚。
- 回滚结果和原因进入证据索引。

### 7.10 离线交付

作为隔离环境交付能力，支持依赖分析、目录清单、真实打包和完整性校核的分阶段建设。

验收要求：

- MVP 能够输出离线依赖分析、包结构和缺失项。
- 真实离线包必须来自已验证交付基线。
- 检查镜像闭包、配置完整性、脚本完整性和版本一致性。
- 支持生成校验和、版本说明和证据索引。

### 7.11 人工确认与风险接受

作为责任边界控制能力，支持计划确认、技术评审、质量评审、交付批准和软门禁风险接受。

验收要求：

- 确认和批准绑定具体对象版本。
- 内容变化后能识别原确认是否失效。
- 风险接受记录原因、影响、补偿措施和有效期。
- 未授权人员不能执行高风险批准。

### 7.12 交付基线与证据归档

作为交付可信和追溯能力，将需求、计划、代码、测试、镜像、部署资源、交付包和人工决策形成版本化关联。

验收要求：

- 每个可交付版本具有明确交付基线。
- 证据记录来源、结果、时间、执行器、环境和完整性摘要。
- 能够从需求追踪到代码、镜像、部署和交付包。
- 能够从部署版本反查需求和批准记录。

---

## 8. 核心对象与状态

### 8.1 核心对象

| 对象 | 说明 |
| --- | --- |
| 项目空间 | 需求、规范、代码上下文、运行、产物和证据的逻辑边界。 |
| 项目规范 | 技术栈、工程结构、测试、安全、构建和交付规则。 |
| 需求项 | 业务目标、范围、验收标准和状态。 |
| 执行策略 | 五维分类结果及对应控制要求。 |
| 执行计划 | 阶段、任务、验证、风险和交付物安排。 |
| Harness Run | 一次受控执行实例。 |
| 阶段 | Run 中可独立推进和验收的步骤。 |
| 智能体任务 | 分配给具体智能体或执行器的任务。 |
| 产物版本 | 代码、测试、配置、镜像、部署资源和报告。 |
| 门禁规则 | 可执行的通过条件和失败动作。 |
| 人工决策 | 确认、驳回、批准和风险接受。 |
| 交付基线 | 已锁定并验证的可交付版本集合。 |
| 离线交付包 | 可搬运、可校验、可部署和可回滚的离线单元。 |
| 证据 | 证明执行事实和结果的结构化记录。 |

### 8.2 需求项状态

```text
草案
  -> 待澄清
  -> 待确认
  -> 已确认
  -> 已变更
  -> 已关闭
```

### 8.3 执行计划状态

```text
草拟中
  -> 待确认
  -> 已确认
  -> 被驳回
  -> 已失效
  -> 已替代
```

### 8.4 Harness Run 状态

```text
待计划
  -> 待确认
  -> 待执行
  -> 执行中
  -> 等待人工输入
  -> 被阻塞
  -> 验证中
  -> 待评审
  -> 通过
  -> 失败
  -> 已取消
  -> 已回退
  -> 已归档
```

### 8.5 阶段状态

```text
待执行
  -> 执行中
  -> 验证中
  -> 已通过
  -> 需修复
  -> 人工处理中
  -> 已跳过
  -> 失败
  -> 已回退
  -> 已取消
```

“已跳过”必须记录规则依据和批准人，不允许跳过硬门禁。

### 8.6 产物版本状态

```text
已生成
  -> 待验证
  -> 已验证
  -> 待批准
  -> 已批准
  -> 已发布
  -> 已替代
  -> 已撤回
```

### 8.7 交付任务状态

```text
待构建
  -> 构建中
  -> 构建失败
  -> 待部署
  -> 部署中
  -> 验证中
  -> 部署通过
  -> 部署失败
  -> 回滚中
  -> 已回滚
  -> 已归档
```

### 8.8 离线交付包状态

```text
待分析
  -> 依赖分析中
  -> 存在缺失项
  -> 待生成
  -> 生成中
  -> 完整性校核中
  -> 校核失败
  -> 待模拟验证
  -> 验证通过
  -> 可交付
  -> 已归档
```

---

## 9. 非功能需求

### 9.1 可追溯性

所有关键动作应关联项目、需求、计划、Run、人员、工具、产物和时间。能够从需求追踪到交付结果，也能够从运行版本反查需求来源。

### 9.2 可审计性

人工确认、驳回、批准、风险接受、门禁调整、重试、跳过、回退和交付动作应保留不可混淆的审计记录。

### 9.3 执行隔离

不同项目和 Run 的代码目录、进程、临时文件和凭据应隔离。智能体不得访问未授权项目和宿主机敏感路径。

### 9.4 权限控制

工具、命令、文件目录、网络、仓库、集群和凭据访问应按项目、智能体和阶段配置最小权限。

### 9.5 提示注入与恶意内容防护

系统应将仓库文件、需求文档和外部知识视为不可信输入，避免其中的指令绕过系统规则、获取凭据或调用未授权工具。

### 9.6 凭据安全

Git、Jenkins、镜像仓库和 Kubernetes 凭据应由安全存储提供，不得写入 Prompt、日志、代码、镜像或交付包。

### 9.7 可靠性

应支持任务超时、幂等重试、断点恢复、异常终止清理和运行状态恢复。平台重启后不应丢失已确认计划、运行状态和关键证据。

### 9.8 资源控制

应支持并发数、CPU、内存、磁盘、运行时长、模型调用次数和 token 用量限制，避免智能体失控消耗资源。

### 9.9 可观测性

应记录 Run、阶段、任务、模型调用、工具调用、文件变更、命令结果、门禁结果和人工操作，并支持按项目和版本检索。

### 9.10 性能

标准基准场景下：

- 创建 Run 后 10 秒内形成可查看的初始状态。
- 阶段和事件状态延迟不超过 5 秒。
- 关键日志和证据在执行完成后 30 秒内可查询。

### 9.11 内网和离线适配

系统应支持部署在内网环境，并接入内网大模型、Git、Jenkins、组件仓库、镜像仓库、制品库和 Kubernetes 集群。核心控制流程不应依赖公网服务。

### 9.12 可扩展性

Agent Runtime、代码仓库、流水线、门禁工具、部署平台和证据采集器应采用适配器方式扩展，避免业务流程绑定某一具体工具。

### 9.13 经济性

应记录不同工程等级的模型调用量、运行时间和资源成本，避免低风险、小规模应用被重型流程拖慢。

---

## 10. MVP 范围

### 10.1 MVP 核心假设

第一阶段需要验证：

> 在限定技术栈和受控运行环境下，Harness 能否将业务需求转化为可运行应用，通过自动验证和反馈修复提高生成成功率，并形成可部署的容器化交付结果。

### 10.2 MVP 主流程

```text
业务需求输入
  -> 需求理解和执行策略建议
  -> 生成实现计划
  -> 人工确认
  -> 智能体生成代码、测试和配置
  -> 构建、启动和核心流程冒烟
  -> 最多两轮自动反馈修复
  -> Docker 镜像构建
  -> 容器环境部署验证
  -> 人工确认交付结果
  -> 形成应用、镜像、运行说明和基础证据
```

### 10.3 MVP 纳入范围

1. 单一项目空间。
2. 一种典型前后端技术栈。
3. 一种数据库。
4. 新建轻量应用作为主试点场景。
5. 自然语言需求输入和结构化需求摘要。
6. 工程等级、风险等级和交付方式建议。
7. 项目规范基础配置和版本绑定。
8. 执行计划生成及人工确认。
9. OpenCode 或等效智能编码引擎调用。
10. 文件变更、命令和工具调用记录。
11. 构建、启动和核心流程冒烟验证。
12. 最多两轮自动修复。
13. 人工接管、取消和重新执行。
14. Dockerfile 生成和镜像构建。
15. 简单容器环境部署验证。
16. 轻量门禁和部分标准门禁。
17. 应用源码、镜像信息、运行说明和基础证据索引。
18. 离线交付依赖分析和目录清单，不包含复杂环境真实交付承诺。

### 10.4 MVP 暂不纳入范围

- 多技术栈全面支持。
- 复杂多智能体自主编排。
- 完整 Kubernetes 生产发布。
- 多集群、多环境统一管理。
- 自动生产环境发布。
- 完整安全扫描体系。
- 真实复杂离线包适配和客户现场部署。
- 完整 SBOM、签名和供应链证明。
- 复杂模板市场。
- 通用低代码页面设计器。
- 完整存量大型项目自动改造。
- 无人值守风险接受和发布批准。

### 10.5 MVP 试点技术栈建议

建议选择一个内部常用且工具链成熟的组合，例如：

- 前端：Vue 3 + Vite。
- 后端：Spring Boot。
- 数据库：PostgreSQL 或当前内网标准数据库。
- 构建：Maven + npm。
- 运行：Docker / Docker Compose。
- 代码仓库：内网 GitLab。
- 流水线：Jenkins。

最终技术栈应根据现有基础设施和试点项目确定，第一阶段不宜并行支持多套组合。

---

## 11. 成功标准与验收场景

### 11.1 业务成功标准

- 用户能够从自然语言需求发起一次快速生成任务。
- 系统能够给出工程等级、风险等级和交付方式建议。
- 用户能够在执行前查看和确认实现计划。
- 系统能够形成可运行应用，而不只是代码文件。
- 系统能够自动执行构建、启动和核心流程冒烟。
- 验证失败后能够驱动智能体进行受控修复。
- 系统能够构建 Docker 镜像并在容器环境完成部署验证。
- 用户能够查看需求、计划、变更、验证、镜像和交付结果之间的关联。

### 11.2 建议量化指标

| 指标 | MVP 建议目标 |
| --- | --- |
| 标准需求首次生成可运行率 | 不低于 60% |
| 两轮修复后的可运行率 | 不低于 80% |
| 生成任务计划可确认率 | 不低于 90% |
| 核心流程冒烟执行率 | 100% |
| 硬门禁失败阻断率 | 100% |
| 可交付版本基础证据完整率 | 100% |
| 镜像与代码版本关联率 | 100% |
| 运行状态和关键事件可查询率 | 100% |
| 标准样例从需求到容器运行时间 | 在基准环境内不超过 60 分钟 |
| 自动修复失控循环 | 0 次 |

指标数值应在试点前结合模型能力、硬件和样例复杂度校准。

### 11.3 基准验收场景一：轻量应用快速生成

输入：

- 一段明确的内部数据录入、查询和统计需求。
- 指定技术栈和字段样例。

验收：

1. 系统生成结构化需求和实现计划。
2. 人工确认后启动 Harness Run。
3. 生成前后端代码、数据库结构、测试和配置。
4. 完成构建和应用启动。
5. 核心录入、查询和统计流程冒烟通过。
6. 构建 Docker 镜像并完成容器部署验证。
7. 输出源码、镜像信息、使用说明和验证证据。

### 11.4 基准验收场景二：生成失败后的反馈修复

输入：

- 一个故意造成构建或测试失败的需求约束或代码版本。

验收：

1. 系统识别失败阶段和失败类型。
2. 展示关键日志和证据。
3. 将失败反馈给智能体。
4. 智能体形成修复变更。
5. 系统重新执行受影响验证项。
6. 自动修复不超过配置上限。
7. 超过上限后进入人工接管。

### 11.5 基准验收场景三：风险强制升级

输入：

- 用户人数较少，但需求涉及敏感数据和审批权限。

验收：

1. 系统不得仅依据人数推荐低风险轻量流程。
2. 系统识别敏感数据和权限风险。
3. 推荐标准或高风险控制。
4. 增加计划确认、安全校核和人工批准。
5. 未通过硬门禁时不得标记为可交付。

### 11.6 基准验收场景四：离线交付分析

输入：

- 一个已通过容器部署验证的应用版本。

验收：

1. 系统识别应用镜像、配置、端口和外部依赖。
2. 输出离线包建议目录和制品清单。
3. 检查部署资源引用的镜像是否完整。
4. 标识当前缺失制品和目标环境待确认项。
5. 输出关联 commit、镜像 digest 和验证结果的离线交付分析报告。

---

## 12. 风险与约束

### 12.1 主题被治理机制淹没

风险：过度强调分类、状态和门禁，导致“快速生成与云原生交付”的业务价值不突出。

应对：始终以需求到应用、应用到交付的主线组织产品和页面，分类与 Harness 作为支撑机制呈现。

### 12.2 MVP 范围失控

风险：同时建设原型、轻量、生产、Kubernetes、离线包和完整证据平台，导致每项能力停留在演示层。

应对：第一阶段聚焦单技术栈下的快速生成、自动验证、反馈修复和容器交付闭环。

### 12.3 模式判断失真

风险：过度依赖人数或用户自选，导致敏感或高风险应用进入轻量流程。

应对：建立风险强制升级规则，人数仅作为辅助判断因素。

### 12.4 AI 生成绕过工程约束

风险：仅依赖 Prompt 生成，结果不符合项目结构、接口和质量要求。

应对：生成任务绑定项目规范、真实代码上下文和可执行门禁。

### 12.5 自动修复无限循环

风险：构建或测试持续失败，智能体反复修改造成资源浪费和代码恶化。

应对：配置重试上限、最小验证集、回退点和人工接管机制。

### 12.6 测试与实现同源偏差

风险：同一智能体同时生成代码和测试，测试可能只验证自身错误假设。

应对：关键验收标准由需求侧确认；重要场景使用独立验证规则、固定基准用例或独立评审智能体。

### 12.7 轻量应用形成影子系统

风险：轻量应用长期运行、扩大使用范围或处理敏感数据后缺乏治理。

应对：记录负责人、数据等级、部署位置、有效期和升级触发条件，超出边界时重新评估。

### 12.8 云原生交付流于资源文件生成

风险：只生成 Dockerfile 和 YAML，未验证镜像、配置和部署是否真实可用。

应对：交付成功必须包含镜像构建、环境部署、健康检查和冒烟验证结果。

### 12.9 离线包来源不可信

风险：直接从开发目录打包，导致代码、镜像、资源和配置版本不一致。

应对：离线包必须来自已验证交付基线，并锁定 commit、镜像 digest、资源版本和校验和。

### 12.10 工具链耦合

风险：产品流程与特定 Git、Jenkins 或 Kubernetes 实现强绑定，后续难以适配。

应对：建立代码仓库、流水线、制品库、部署和证据采集适配器。

### 12.11 敏感信息泄露

风险：凭据被写入 Prompt、日志、代码、镜像层或交付包。

应对：使用凭据引用和短期授权，执行输出脱敏、镜像层检查和交付包敏感项检查。

### 12.12 证据只是日志汇总

风险：证据缺乏来源、摘要和对象关联，无法支撑审计和版本追踪。

应对：结构化记录来源系统、执行环境、产物摘要、工具版本、时间和替代关系。

---

## 13. 后续待确认事项

进入总体设计和研发拆分前，应确认：

1. MVP 试点业务场景和标准需求样例。
2. 第一阶段唯一技术栈和数据库。
3. OpenCode 的运行、工作区和工具权限方式。
4. 项目规范通过配置文件、页面还是两者结合维护。
5. 轻量、标准和严格门禁的默认规则。
6. 自动修复次数、超时和资源上限。
7. Docker 镜像构建和容器部署验证环境。
8. GitLab、Jenkins、组件仓库和镜像仓库接入范围。
9. 人工确认角色和审批权限。
10. 证据保留周期、脱敏和归档位置。
11. 离线交付第一阶段只做分析，还是选择一个试点完成真实打包。
12. MVP 量化指标的最终目标值和验收数据集。

---

## 14. 第一阶段交付物建议

第一阶段研发完成后，应至少形成：

- 快速生成与云原生交付总体设计。
- 单一技术栈项目规范模板。
- Harness Run 和阶段状态模型。
- 执行策略与计划数据模型。
- OpenCode 执行接入能力。
- 文件、命令、工具和产物采集能力。
- 构建、启动和冒烟门禁执行器。
- 自动反馈修复和人工接管机制。
- Dockerfile 生成与镜像构建能力。
- 简单容器环境部署验证能力。
- 交付基线和基础证据索引。
- 至少两组端到端验收样例及结果报告。

---

## 15. 总结

快速生成与云原生交付的建设重点，不是简单提升代码生成数量，也不是为所有应用叠加统一的重型交付流程。

其核心是建立一条受控闭环：

```text
让需求更快形成应用，
让应用经过验证后形成标准交付物，
让失败能够被解释和修复，
让不同风险使用不同控制强度，
让每个可交付版本都能够追溯到需求、代码、镜像、部署和人工决策。
```

在该闭环中：

- 快速生成是效率能力。
- 云原生交付是工程落地能力。
- 工程等级和风险等级是分级策略。
- Harness 是过程控制和可信保障底座。

第一阶段应以“单一技术栈、轻量应用、容器交付”为切入点，优先验证从需求输入到可运行应用、从应用生成到容器部署验证的完整价值，再逐步扩展生产级 Kubernetes、复杂存量项目和真实离线交付能力。
